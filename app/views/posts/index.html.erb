<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title></title>
</head>
<body>
  <h1 style="text-align: center;">All<span class="text-danger"> Posts</span></h1>
  <table class="table">
   <thead>
    <tr>
      <th scope="col">Title</th>
      <th scope="col">Description</th>
      <th scope="col">Location</th>
    </tr>
  </thead>
  <tbody>
     <% @posts.each do |post| %>
     
    <tr>
      <td> <%= link_to post.title, user_post_path(post.user_id, post.id) %></td>
      <td><%= link_to post.description,  user_post_path(post.user_id, post.id) %></td>
      <td><%= link_to post.location,  user_post_path(post.user_id, post.id) %></td>

      <!-- <td><#%= image_tag i.avatar, size:122 if i.avatar.attached? %></td> -->
    </tr> 
  </tbody>
  <% end %>
</table>
<p style="text-align: center;">
<%= link_to 'Back', root_path, class:"btn btn-warning" %>   
</p>

</body>
</html>

